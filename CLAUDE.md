# Dev Decisions

這是 justmaker 專案群的開發決策記錄 repo。

## 規則

- 所有文件使用繁體中文
- 決策記錄放在 `decisions/` 目錄，依序編號
- 每新增一筆決策，同步更新 `README.md` 索引
- 模板在 `templates/`，不要直接修改模板
- 相關 repo 用 `justmaker/repo-name` 格式標注
- GitHub 操作一律使用 justmaker 身分
